# Every change needs a maintainer review.
#
# An unresolvable owner here silently stops review requests from being assigned,
# so keep these handles current if maintainership changes.
* @mchittineni

# The public contract. Changing these breaks consumers or the sibling repository,
# so they warrant a deliberate second look.
/action.yml                @mchittineni
/shared/types.ts           @mchittineni
/.github/workflows/        @mchittineni

# Generated bundle — reviewed as a build artifact, never edited by hand.
/dist/                     @mchittineni
